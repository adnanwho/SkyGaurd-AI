# Evaluation

Evaluation uses deterministic simulated observations, injected isolated faults, and regional weather-event scenarios. Results are written to `outputs/evaluation/latest.csv` by `src/skyguard/evaluation/runner.py`.

Run it with:

```powershell
python main.py --evaluate
```

Metrics are measured outputs and must not be replaced with assumed or fabricated values.
